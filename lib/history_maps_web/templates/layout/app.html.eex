<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <meta name="description" content="">
    <meta name="author" content="">

    <title>History Maps</title>
    <link rel="stylesheet" href="<%= static_path(@conn, "/css/app.css") %>">
  </head>

  <body>
    <div class="container">
      <nav class="navbar navbar-default">
        <div class="container-fluid">
          <div class="navbar-header">
            <a class="navbar-brand" href="#">History Maps</a>
          </div>
          <ul class="nav navbar-nav">
            <li><%= link "Home", to: page_path(@conn, :index) %></li>
            <%= if @current_user do %>
              <li><%= link "Maps", to: page_path(@conn, :index) %></li>
              <li><%= link "Locations", to: page_path(@conn, :index) %></li>
          </ul>
          <ul class="nav navbar-nav navbar-right">
            <li class="navbar-text"><strong class="text-danger"><%= Mix.env |>
            to_string() %></strong></li>
            <li class="navbar-text"><%= @current_user.username %></li>
            <li class="navbar-text"><%= link "Log out", to: session_path(@conn, :delete, @current_user), method: "delete" %></li>
          </ul>
        <% else %>
          <ul class="nav navbar-nav navbar-right">
          <li><%= link "Register", to: user_path(@conn, :new) %></li>
          <li><%= link "Log in", to: session_path(@conn, :new) %></li>
          </ul>
        <% end %>
        </div>
      </nav>
      </ol>
        <span class="logo"></span>
      <p class="alert alert-info" role="alert"><%= get_flash(@conn, :info) %></p>
      <p class="alert alert-danger" role="alert"><%= get_flash(@conn, :error) %></p>
      </div>

      <div class="container">
        <div class="row">
          <div class="col-md-2">
          </div>
          <div class="col-md-7">
      <main role="main">
        <%= render @view_module, @view_template, assigns %>
      </main>
          </div>
          <div class="col-md-3">
          </div>
        </div>
      </div>


    </div> <!-- /container -->
    <script src="<%= static_path(@conn, "/js/app.js") %>"></script>
  </body>
</html>
